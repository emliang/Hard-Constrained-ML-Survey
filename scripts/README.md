
# Scripts

These scripts are lightweight maintenance helpers for the repository.

They are not required for reading the guide.

- `validate_data.py`: checks method metadata against the BibTeX snapshot.
- `check_bib_keys.py`: checks duplicate BibTeX keys.
- `build_method_cards.py`: regenerates compact method-card pages from `data/methods.yaml`.
